# Repository Rules

- Keep the bundled demo dataset anonymized. Do not commit real cluster names, node names, domains, IPs, or credentials.
- Real snapshot data must come from ignored local files or an environment-configured path such as `GKE_DASHBOARD_SNAPSHOT_PATH`.
- Run `pnpm verify` before claiming the work is complete.
